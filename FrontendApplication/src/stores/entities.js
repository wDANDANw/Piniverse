import { defineStore } from "pinia";

export const useEntityStore = defineStore("entities", {
    state: () => {

    }
})

// Object carrying info:
// Entity Name
// Entity Ontology / Adjs
// Entity
// Entity Behaviors // Not so far

// Renderer side:
// Entity points array / object3d / src
// Entity type
// Entity mesh
